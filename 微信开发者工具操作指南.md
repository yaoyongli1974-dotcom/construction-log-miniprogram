# 微信开发者工具操作指南

> 本文档手把手教你下载、安装、配置微信开发者工具，并导入"施工日志"小程序项目。

---

## 一、下载与安装

### 1.1 下载微信开发者工具

**官方下载地址**：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

**选择对应版本**：
- **Windows 64位**：大多数Windows电脑选择这个
- **Windows 32位**：老式电脑选择这个
- **macOS**：苹果电脑选择这个

**建议**：下载**稳定版（Stable Build）**，不要下载Beta版。

---

### 1.2 安装步骤

#### Windows用户：
1. 双击下载的 `.exe` 安装包
2. 点击 **"下一步"**
3. 选择安装路径（建议默认 `C:\Program Files\`）
4. 点击 **"安装"**
5. 等待安装完成，点击 **"完成"**

#### macOS用户：
1. 双击下载的 `.dmg` 文件
2. 将 **"微信开发者工具"** 拖到 **"Applications"** 文件夹
3. 打开启动台，找到并打开 **"微信开发者工具"**

---

## 二、注册小程序账号

> ⚠️ **重要**：如果没有小程序账号，无法正式发布小程序！

### 2.1 注册流程

1. 打开 **微信公众平台**：https://mp.weixin.qq.com/
2. 点击右上角 **"立即注册"**
3. 选择 **"小程序"**
4. 填写邮箱、密码等信息
5. 激活邮箱（查收激活邮件）
6. 选择 **"个人"** 主体类型（个人开发者选这个）
7. 填写管理员信息（微信扫码验证）
8. 提交审核（个人主体无需认证费）

**注册完成时间**：约10分钟

---

### 2.2 获取AppID

1. 登录 **微信公众平台**：https://mp.weixin.qq.com/
2. 进入 **"开发"** → **"开发设置"**
3. 找到 **"AppID(小程序ID)"**
4. 点击 **"生成"** 或复制已有的AppID

**示例AppID格式**：`wx1234567890abcdef`

**保存AppID**：记录下来，后面导入项目需要用到。

---

## 三、导入"施工日志"小程序项目

### 3.1 打开微信开发者工具

1. 双击桌面图标 **"微信开发者工具"**
2. 使用 **微信扫码登录**（管理员微信）

---

### 3.2 导入项目

1. 在欢迎界面，点击 **"+ 新建项目"**
2. 填写项目信息：

| 字段 | 填写内容 | 说明 |
|------|----------|------|
| **项目名称** | `施工日志` | 随便取，方便识别 |
| **目录** | `D:\workbuddy\微信小程序\1.施工日志` | 点击"选择"，浏览到项目目录 |
| **AppID** | `你的AppID` | 从微信公众平台获取 |
| **开发模式** | `小程序` | 默认选项 |
| **后端服务** | `微信云开发` | **重要！必须选这个** |
| **模板选择** | `不使用云服务` | 我们已经有云函数，不用模板 |

3. 点击 **"新建"**

**成功标志**：进入开发者工具主界面，左侧是文件树，中间是代码编辑器，右侧是模拟器。

---

### 3.3 解决导入常见问题

#### 问题1：AppID无效
- **原因**：AppID填写错误或未注册小程序
- **解决**：重新从微信公众平台复制AppID

#### 问题2：目录已存在项目
- **原因**：之前导入过同名项目
- **解决**：点击 **"项目"** → **"关闭项目"**，然后重新导入

#### 问题3：云开发环境未初始化
- **原因**：首次使用云开发
- **解决**：见下一节 **"四、配置云开发"**

---

## 四、配置云开发

> **云开发是"施工日志"小程序的核心**，所有数据存储在云端。

### 4.1 开通云开发

1. 在开发者工具主界面，点击顶部 **"云开发"** 按钮
2. 如果是首次使用，会弹出 **"开通云开发"** 窗口
3. 点击 **"开通"**
4. 填写云开发环境名称：
   - **环境名称**：`施工日志云环境`（随便取）
   - **环境ID**：自动生成，如 `cloud1-xxxxxx`
5. 点击 **"确定"**
6. 等待开通（约1-2分钟）

**开通成功标志**：打开云开发控制台窗口。

---

### 4.2 记录云环境ID

1. 在云开发控制台，点击右上角 **"设置"** 图标
2. 找到 **"环境ID"**
3. 复制环境ID（如 `cloud1-xxxxxx`）

**保存环境ID**：记录下来，下一步需要用到。

---

### 4.3 修改项目配置（重要！）

1. 在开发者工具中，打开文件 `app.js`
2. 找到以下代码（约第7-11行）：

```javascript
// 初始化云开发
wx.cloud.init({
  env: 'your-env-id',  // ←← 修改这里
  traceUser: true
});
```

3. 将 `your-env-id` 替换为你的云环境ID：

```javascript
wx.cloud.init({
  env: 'cloud1-xxxxxx',  // ←← 替换为你的环境ID
  traceUser: true
});
```

4. 按 `Ctrl + S` 保存文件

---

## 五、上传云函数

> **云函数是小程序的后端逻辑**，必须上传到云端才能使用。

### 5.1 上传单个云函数

1. 在开发者工具左侧文件树，找到 `cloudfunctions` 文件夹
2. 展开文件夹，看到6个云函数：
   - `login`
   - `createLog`
   - `getLogs`
   - `getLogDetail`
   - `uploadImage`
   - `getWeather`

3. **右键点击** `login` 文件夹
4. 选择 **"上传并部署：云端安装依赖"**
5. 等待上传完成（底部会有提示）

**重复以上步骤**，依次上传其他5个云函数。

---

### 5.2 验证云函数上传成功

1. 打开 **云开发控制台**（点击顶部 **"云开发"** 按钮）
2. 左侧菜单选择 **"云函数"**
3. 确认看到6个云函数：
   - login
   - createLog
   - getLogs
   - getLogDetail
   - uploadImage
   - getWeather

**成功标志**：6个云函数状态都是 **"部署成功"**。

---

## 六、初始化数据库

> **数据库用于存储用户信息、施工日志等数据**。

### 6.1 创建集合（数据表）

1. 打开 **云开发控制台**
2. 左侧菜单选择 **"数据库"**
3. 点击 **"+ 创建集合"**
4. 依次创建以下5个集合：

| 集合名称 | 说明 |
|----------|------|
| `users` | 用户表 |
| `logs` | 施工日志表 |
| `projects` | 项目表（后期扩展用） |
| `shares` | 分享记录表 |
| `rewards` | 奖励记录表 |

**创建方法**：输入集合名称，点击 **"确定"**。

---

### 6.2 设置数据库权限

1. 在 **"数据库"** 页面，点击任意集合名称
2. 选择 **"权限设置"** 标签页
3. 修改权限为 **"所有用户可读，仅创建者可读写"**
4. 点击 **"保存"**

**重复以上步骤**，为所有集合设置权限。

---

## 七、编译与预览

### 7.1 编译项目

1. 在开发者工具主界面，点击顶部 **"编译"** 按钮
2. 等待编译完成（底部会有提示）
3. 查看右侧 **模拟器**，确认界面正常显示

**成功标志**：
- 底部TabBar显示3个标签：日志、新建、我的
- 首页显示日志列表（当前为空）
- 无红色错误信息

---

### 7.2 真机预览

> **模拟器显示正常不代表真机正常**，必须在手机上测试！

1. 点击顶部 **"预览"** 按钮
2. 选择 **"扫码预览"**
3. 使用微信扫描二维码
4. 在手机上体验小程序

**测试要点**：
- [ ] 微信一键登录是否成功
- [ ] 新建日志页面是否正常
- [ ] 拍照上传功能是否可用
- [ ] 保存日志是否成功
- [ ] 查看日志详情是否正常

---

### 7.3 常见问题解决

#### 问题1：编译报错"云环境ID无效"
- **原因**：`app.js` 中的环境ID未修改
- **解决**：按照 **"四、配置云开发 / 4.3"** 修改环境ID

#### 问题2：调用云函数失败
- **原因**：云函数未上传或上传失败
- **解决**：按照 **"五、上传云函数"** 重新上传

#### 问题3：数据库读写失败
- **原因**：集合未创建或权限设置错误
- **解决**：按照 **"六、初始化数据库"** 创建集合并设置权限

#### 问题4：真机预览无法正常使用
- **原因**：手机微信版本过低或网络问题
- **解决**：更新微信到最新版本，切换网络重试

---

## 八、界面介绍（快速上手）

### 8.1 开发者工具主界面

```
┌─────────────────────────────────────────────────┐
│  菜单栏（项目、文件、编辑...）                 │
├──────────┬──────────────────────┬──────────────┤
│          │                      │              │
│  文件树   │    代码编辑器          │   模拟器      │
│  (左侧)  │    (中间)            │   (右侧)      │
│          │                      │              │
│  pages/  │  // 代码显示区域      │  ┌────────┐  │
│  components/                   │  │  手机界面 │  │
│  cloudfunctions/              │  │   模拟   │  │
│          │                      │  └────────┘  │
├──────────┴──────────────────────┴──────────────┤
│  底部提示栏（编译状态、错误信息）                │
└─────────────────────────────────────────────────┘
```

---

### 8.2 常用按钮

| 按钮 | 位置 | 功能 |
|------|------|------|
| **编译** | 顶部工具栏 | 重新编译项目，查看效果 |
| **预览** | 顶部工具栏 | 生成二维码，手机扫码体验 |
| **真机调试** | 顶部工具栏 | 在手机上调试代码 |
| **上传** | 顶部工具栏 | 上传代码版本，用于提交审核 |
| **云开发** | 顶部工具栏 | 打开云开发控制台 |
| **详情** | 右上角 | 查看项目配置、AppID等信息 |

---

### 8.3 调试技巧

#### 查看控制台日志
1. 点击模拟器下方的 **"Console"** 标签页
2. 所有 `console.log()` 的输出都会显示在这里
3. 用于调试代码、查看错误信息

#### 断点调试
1. 在代码编辑器中，点击行号左侧的空白区域
2. 会出现红色断点标记
3. 点击 **"调试器"** 按钮，运行到断点会暂停
4. 可以查看变量值、单步执行

---

## 九、提交审核与发布

> **只有提交审核并通过后，用户才能在微信中搜索到你的小程序！**

### 9.1 上传代码版本

1. 在开发者工具中，点击顶部 **"上传"** 按钮
2. 填写版本号：`1.0.0`
3. 填写项目备注：`首次提交，施工日志MVP版本`
4. 点击 **"上传"**
5. 等待上传完成

---

### 9.2 提交审核

1. 登录 **微信公众平台**：https://mp.weixin.qq.com/
2. 进入 **"版本管理"**
3. 找到刚上传的版本，点击 **"提交审核"**
4. 填写审核信息（参考 `审核材料/审核提交信息.md`）
5. 上传截图（用手机预览截图）
6. 点击 **"提交审核"**

**审核时间**：通常1-3天

---

### 9.3 发布上线

1. 审核通过后，微信会发送通知
2. 登录 **微信公众平台**
3. 进入 **"版本管理"**
4. 点击 **"发布"** 按钮
5. 小程序正式上线！

---

## 十、常见问题FAQ

### Q1：我是新手，看不懂这些步骤怎么办？
**A**：建议先看微信官方视频教程：https://developers.weixin.qq.com/miniprogram/dev/framework/ （"起步"部分）

---

### Q2：没有AppID可以用测试号吗？
**A**：可以！导入项目时，选择 **"使用测试号"**，但无法正式发布，只能自己测试。

---

### Q3：云开发收费吗？
**A**：有免费额度！个人开发够用：
- 数据库：5GB免费存储
- 云函数：4GB免费调用次数
- 云存储：5GB免费空间

超出后按量收费，但个人小程序很难超出。

---

### Q4：上传云函数时提示"依赖安装失败"？
**A**：可能是网络问题，尝试：
1. 更换网络（用手机热点）
2. 重新上传
3. 在云函数目录手动运行 `npm install`

---

### Q5：真机预览时照片上传失败？
**A**：检查以下配置：
1. 云存储权限是否设置为 **"所有用户可读，仅创建者可读写"**
2. 手机网络是否正常
3. 微信是否已授权相机和相册权限

---

## 十一、下一步行动清单

### ✅ 今天完成（预计1小时）

- [ ] 下载并安装微信开发者工具
- [ ] 注册小程序账号（如没有）
- [ ] 获取AppID
- [ ] 导入"施工日志"项目
- [ ] 开通云开发
- [ ] 修改 `app.js` 中的云环境ID
- [ ] 上传6个云函数
- [ ] 创建5个数据库集合
- [ ] 点击"编译"，查看模拟器是否正常

---

### ✅ 明天完成（预计30分钟）

- [ ] 真机预览，测试所有功能
- [ ] 截取功能页面图（用于审核）
- [ ] 填写审核信息（参考 `审核材料/审核提交信息.md`）
- [ ] 提交审核

---

### ✅ 审核通过后（预计5分钟）

- [ ] 点击"发布"按钮
- [ ] 在微信中搜索"施工日志"，确认能搜到
- [ ] 分享给朋友，邀请试用

---

## 十二、技术支持

### 官方文档
- **小程序开发文档**：https://developers.weixin.qq.com/miniprogram/dev/framework/
- **云开发文档**：https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html
- **审核指南**：https://developers.weixin.qq.com/miniprogram/dev/framework/audit/

### 社区论坛
- **微信开放社区**：https://developers.weixin.qq.com/community/

---

## 附录：快捷键一览

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + S` | 保存文件 |
| `Ctrl + B` | 编译项目 |
| `Ctrl + R` | 刷新模拟器 |
| `Ctrl + Shift + F` | 全局搜索 |
| `Ctrl + P` | 快速打开文件 |
| `F12` | 打开调试器 |

---

**文档版本**：v1.0  
**最后更新**：2026-06-17  
**适用项目**：施工日志微信小程序  
**作者**：微信小程序开发工程师
