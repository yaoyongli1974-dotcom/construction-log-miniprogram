# 施工日志小程序 - 上线指南

## 🚀 上线流程总览

```
上线准备（1-2天）
    ↓
上传云函数 + 数据库初始化（1天）
    ↓
真机测试 + 修复Bug（1-2天）
    ↓
提交审核（微信审核 1-7天）
    ↓
审核通过 → 发布上线 ✅
```

---

## 第一步：上线前检查清单

### ✅ 必须完成的项目

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 所有页面开发完成 | ✅ 已完成 | 5个页面（首页、新建/编辑、详情、个人中心、分享） |
| 云函数上传完成 | ❌ 待完成 | 需上传所有云函数到云端 |
| 数据库初始化 | ❌ 待完成 | 创建集合 + 配置权限 |
| 导出功能测试 | ❌ 待完成 | Word/Excel导出需测试 |
| 隐私协议 | ❌ 待完成 | 需完善隐私政策和用户协议 |
| 小程序类目 | ❌ 待完成 | 需在微信后台设置正确类目 |

### ⚠️ 建议完成的项目

| 检查项 | 优先级 | 说明 |
|--------|----------|------|
| 性能优化 | 中 | 确保启动时间 < 1.5秒 |
| 错误处理 | 高 | 所有网络请求需有错误提示 |
| 边界测试 | 高 | 测试空数据、网络异常等情况 |
| 权限申请 | 高 | 确保只申请必要的权限 |

---

## 第二步：初始化数据库（必须）

### 2.1 创建集合

1. 打开**微信开发者工具**
2. 点击左侧「云开发」图标
3. 进入「数据库」标签
4. 点击「+」创建以下集合：

| 集合名称 | 用途 | 权限设置 |
|----------|------|----------|
| `users` | 存储用户信息 | 仅创建者可读写 |
| `logs` | 存储施工日志 | 仅创建者可读写 |
| `login_logs` | 存储登录日志 | 仅创建者可读写 |

### 2.2 配置集合权限

对每个集合，点击「权限设置」，选择：
- **仅创建者可读写** （推荐）
- 或者 **所有用户可读，仅创建者可读写**

### 2.3 创建索引（可选，优化查询速度）

在 `logs` 集合创建索引：
- `userId` (升序) + `date` (降序) → 用于按日期查询日志
- `userId` (升序) + `createTime` (降序) → 用于分页查询

---

## 第三步：上传云函数（必须）

### 3.1 上传所有云函数

在微信开发者工具中：
1. 找到 `cloudfunctions/` 目录
2. 对每个云函数文件夹，**右键** → 「上传并部署：云端安装依赖」

**需要上传的云函数列表：**

| 云函数名 | 功能 | 依赖 |
|----------|------|------|
| `login` | 用户登录 | 无 |
| `createLog` | 创建日志 | 无 |
| `getLogs` | 获取日志列表 | 无 |
| `updateLog` | 更新日志 | 无 |
| `deleteLog` | 删除日志 | 无 |
| `exportWord` | 导出Word | `docx` |
| `exportExcel` | 导出Excel | `exceljs` |

### 3.2 测试云函数

上传完成后，测试每个云函数：
1. 在云开发控制台 → 「云函数」
2. 点击某个云函数 → 「测试」
3. 输入测试参数，运行测试

---

## 第四步：配置小程序（必须）

### 4.1 设置小程序类目

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 「设置」→「基本设置」→「服务类目」
3. 选择类目：**工具** → **办公** 或 **商务服务** → **企业管理**

> ⚠️ 类目选择会影响审核！建议选择：**工具 > 办公**

### 4.2 配置服务器域名（云开发不需要）

使用云开发的小程序**不需要**配置服务器域名，跳过此步。

### 4.3 配置隐私协议

1. 在 `app.json` 中添加隐私协议提示：
```json
{
  "pages": [...],
  "隐私设置": {
    "requiredPrivateInfos": []
  }
}
```

2. 在「微信公众平台」→「设置」→「基本设置」→「隐私协议」中，填写隐私政策链接

---

## 第五步：真机测试（必须）

### 5.1 真机调试

1. 微信开发者工具 → 点击「真机调试」
2. 用手机微信扫描二维码
3. 测试所有功能：

**测试清单：**
- [ ] 用户登录（是否正常获取open_id）
- [ ] 创建日志（填写完整信息 + 保存）
- [ ] 编辑日志（修改内容 + 保存）
- [ ] 删除日志（确认删除 + 列表更新）
- [ ] 导出Word（下载 + 用WPS打开检查格式）
- [ ] 导出Excel（下载 + 用WPS打开检查格式）
- [ ] 分享功能（分享到微信好友）
- [ ] 离线状态（断网后是否能正常提示）

### 5.2 多设备测试

| 设备类型 | 测试重点 |
|----------|----------|
| iOS（iPhone） | 兼容性、样式是否正常 |
| Android（华为/小米） | 性能、键盘弹出是否正常 |
| 平板（iPad） | 布局是否适配 |

---

## 第六步：准备审核材料

### 6.1 准备截图/视频

微信审核需要提供的材料：

1. **功能演示视频**（必须）
   - 录制时长：30秒 - 1分钟
   - 内容：登录 → 创建日志 → 保存 → 导出
   - 保存路径：`miniprogram/images/review-demo.mp4`

2. **关键页面截图**（可选）
   - 首页、新建页、详情页、个人中心
   - 保存路径：`miniprogram/images/review-screenshots/`

### 6.2 填写审核信息

在微信开发者工具中提交审核时，需要填写：

| 字段 | 填写内容 |
|------|----------|
| 功能页面 | `pages/index/index` |
| 标题 | 施工日志 - 智能化工地管理工具 |
| 服务类目 | 工具 > 办公 |
| 标签 | 施工日志,工地管理,工程记录 |
| 审核说明 | 本小程序用于施工日志的记录和导出，帮助施工人员高效管理工作日志。 |

---

## 第七步：提交审核

### 7.1 上传代码

1. 微信开发者工具 → 「上传」
2. 填写版本号：`1.0.0`
3. 填写项目备注：「首次提交，施工日志核心功能完成」

### 7.2 提交审核

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 「管理」→「版本管理」
3. 找到刚上传的版本 → 点击「提交审核」
4. 填写审核信息（参考第六步）
5. 上传演示视频
6. 点击「提交审核」

### 7.3 等待审核

- 审核时间：**1-7个工作日**
- 审核结果会通过「微信公众平台」服务通知推送

---

## 第八步：发布上线

### 8.1 审核通过后

1. 收到审核通过通知
2. 登录 [微信公众平台](https://mp.weixin.qq.com/)
3. 「管理」→「版本管理」
4. 点击「发布」→ 确认发布

### 8.2 发布后检查

1. 用手机微信搜索小程序名称
2. 确认能正常搜索到
3. 进入小程序，测试所有功能

---

## ⚠️ 常见审核拒绝原因 + 解决方案

| 拒绝原因 | 解决方案 |
|----------|----------|
| 类目不符 | 修改服务类目为「工具 > 办公」 |
| 功能不完善 | 确保所有按钮都能正常点击 |
| 隐私协议不完整 | 在公众平台完善隐私政策 |
| 存在测试数据 | 清除所有测试数据 |
| 页面空白/报错 | 真机测试确保所有页面正常 |
| 诱导分享 | 不要强制用户分享才能使用 |

---

## 📞 需要帮助？

如果在上线过程中遇到任何问题，可以：
1. 查看微信官方文档：[微信小程序审核指南](https://developers.weixin.qq.com/miniprogram/dev/framework/audit.html)
2. 联系我（WorkBuddy），提供具体错误信息，我会帮你解决！

---

## 🎉 预计上线时间

| 阶段 | 预计时间 |
|------|----------|
| 数据库初始化 + 云函数上传 | 1天 |
| 真机测试 + Bug修复 | 1-2天 |
| 微信审核 | 1-7天 |
| **总计** | **3-10天** |

加油！按照这个指南操作，你的施工日志小程序很快就能上线了！ 🚀
